version: '1'
process: test
model: './path/to/model.ext'
input:
  file:
    type: features
    filename: './path/to/file.ext'
windowing:
  enable: false
features:
  - label: average
    container: core-features
  - label: std_dev
    container: core-features
algorithm:
  name: random_forest
  container: core-py-sk
  parameters:
    example: parameter

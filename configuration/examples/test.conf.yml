version: '1'
process: test
model: './path/to/model.ext'
input:
  file:
    type: features
    filename: './path/to/file.ext'
windowing:
  enable: false
features:
  - average
  - std_dev
algorithm:
  name: random_forest
  parameters:
    example: parameter
